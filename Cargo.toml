[package]
name = "benchmark"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.52.3", features=["full"] }
futures = "*"
tracing = "*"
tracing-subscriber = "*"
rand = "*" # for rwlock random read/write